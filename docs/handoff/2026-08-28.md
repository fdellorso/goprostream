# Handoff — 2026-08-28

## Sessione: Analisi crash container + fix nginx + piano fix

### Obiettivo
Investigare la regressione del crash del container `goprostream`, documentare le cause,
applicare i fix non-risk e creare un piano di fix per i bug rimanenti.

### Cosa è stato fatto

#### 1. Analisi completa delle regressioni
- Confronto git tra commit `42b0726` (pre-regressione) e `HEAD`
- Identificate 9 cause di regressione (8 dal codice + 1 emersa dall'analisi)
- Classificate in 🔧 Bug diretti (3) e ⚙️ Rilassamenti sistema (4)
- Documentazione completa: `docs/analisi-crash-container.md`

#### 2. Approfondimenti
- **`network_mode: host`**: 5 opzioni analizzate per rimuoverlo senza riabilitare il container API
- **KeepAlive multiprocessing**: analisi orfani (risultato: non è il problema)
- **`time.sleep(10)`**: analisi con references GoPro, concluded che il WiFi non si disconnette
- **`hls_fragment 1`**: analisi teorica + dato empirico (latenza 5-6s, non 2s teoriche)
- **FFmpeg zombie**: 4 bug identificati (`shell=True`, `wait()` mancante, etc.)
- **Skill nginx-rtmp-module**: creata con direttive, best practice, default ufficiali

#### 3. Fix applicati
- **nginx.conf**: `hls_fragment` 1→3, `hls_playlist_length` 3→10, `hls_sync` 100ms, `drop_idle_publisher` 30s, `idle_streams off`, `ping 30s`, `allow/deny publish`
- **Pipfile**: `goprocam = "==4.2.0"` (pinna versione)
- **Dockerfile.python**: rimosso fallback `|| pip install goprocam`

#### 4. Piano fix crash
- 5 fix documentati con pseudocodice e ordine di esecuzione
- Testing plan su OUYA
- Documentazione: `docs/fix-crash-container.md`

#### 5. Skill nginx-rtmp-module
- Creata `.pi/skills/nginx-rtmp-module/SKILL.md` (350 righe)
- Direttive complete con default ufficiali e il nostro uso
- Best practice dalla wiki ufficiale
- Fix consigliati prioritizzati

### File modificati
- `docker/nginx.conf` — fix HLS, sicurezza, keepalive
- `python/Pipfile` — pin goprocam ==4.2.0
- `docker/Dockerfile.python` — rimosso fallback

### File creati
- `docs/analisi-crash-container.md` — analisi completa regressioni
- `docs/fix-crash-container.md` — piano fix crash container
- `.pi/skills/nginx-rtmp-module/SKILL.md` — skill nginx-rtmp-module

### Stato attuale

| Fix | Stato |
|-----|-------|
| nginx.conf (HLS, sicurezza, keepalive) | ✅ Applicato |
| Pipfile pin ==4.2.0 | ✅ Applicato |
| Dockerfile rimosso fallback | ✅ Applicato |
| Supervisore `_check_rtmp_socket()` | ❌ Da fixare |
| FFmpeg zombie (shell=True + wait) | ❌ Da fixare |
| Network mode host | ❌ Da valutare |
| Monitor keepalive | ❌ Da fixare |

### Prossimi passi
1. Applicare Fix 2+3 (zombie FFmpeg)
2. Applicare Fix 1 (supervisore stati)
3. Test su OUYA
4. Valutare Fix 4 (network mode) solo se necessario

### Note per la prossima sessione
- Il bug principale è il supervisore `_check_rtmp_socket()` che killa FFmpeg per stati transitori normali
- Gli zombie si accumulano perché manca `wait()` dopo SIGKILL
- Con i fix nginx applicati, il circolo vizioso (hls_fragment 1 → micro-lag → drop → kill) si è rotto parzialmente
- La skill nginx-rtmp-module è un riferimento utile per configurazioni future
